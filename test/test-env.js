import * as androidlib from '../src/index';
import { expect } from 'chai';
import fs from 'fs';
import path from 'path';

describe('detect', () => {
	it('should ???', function (done) {
		this.timeout(5000);
		this.slow(4000);

		done();
	});
});
