/**
 * Determines if the device info is an emulator or a real device.
 *
 * @param {Object} info - The device info.
 * @param {Object} props - A dictionary of properties from the device.
 * @returns {Promise<Boolean>}
 */
export async function isEmulator(info, props) {
	// TODO

	return false;
}
